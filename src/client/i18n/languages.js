export const DEFAULT_LOCALE = ((navigator.languages && navigator.languages[0]) || navigator.language || navigator.userLang || 'en').toLowerCase()

export const SUPPORT_LANG = {
  en: 'en',
  'en-us': 'en',
  'zh-cn': 'zh'
}
